# IBDA Webinar

אתר הרשמה ותשלומים לסדרת הוובינרים של משרד עו״ד יפעת בן דוד עמית.
TanStack Start v1 + Postgres (postgres.js) + Resend + Sumit, פרוס ב-Railway.

## מעקב שעות — חשוב לכל סשן

בפרויקט הזה מתנהל **בנק שעות** מול הלקוחה: 50 שעות תמורת 19,000 ש״ח.
המצב המצטבר נמצא **במסד הנתונים**, לא בקובץ הזה, ומוצג בעמוד המשותף `/partner`.

**בסוף כל מקטע עבודה משמעותי, וכן בסוף הסשן, יש להריץ:**

```bash
node scripts/sync-hours.mjs --describe "תיאור קצר של מה שנעשה" \
                            --details "פירוט (לא חובה)"
```

הסקריפט מודד את זמן העבודה הפעיל של הסשן הנוכחי מתוך תמליל הסשן,
בניכוי פערי המתנה מעל 15 דקות, ושולח לעמוד. הכתיבה אידמפוטנטית
(מפתח: סשן + יום), אז אפשר להריץ כמה פעמים באותו סשן בלי ליצור כפילויות.

- `--status` מציג את המדידה והיתרה בלי לשלוח.
- `--dry-run` מודד ומדפיס בלי לשלוח.
- דורש `RETAINER_SYNC_TOKEN` בסביבה.

**למה בסוף כל מקטע ולא רק בסוף:** תמליל הסשן נמחק יחד עם הקונטיינר. מה
שכבר נשלח למסד הנתונים שמור לתמיד, ומה שלא נמדד בזמן — אבד.

**את התיאור כותב Claude, לא הלקוחה.** המספר מגיע מהמדידה, הטקסט מגיע ממה
שבאמת נעשה בסשן. עבודה שקרתה מחוץ לסביבה (פגישות, שיחות) מוזנת ידנית
דרך העמוד ומסומנת שם כ״ידני״.

## פקודות

```bash
npx tsc --noEmit                          # בדיקת טיפוסים
NITRO_PRESET=node-server npx vite build   # בילד
npx eslint src                            # בסיס ידוע: 207 בעיות. אין להוסיף חדשות.
```

## מוסכמות

- **עברית בלבד** בכל טקסט שמוצג למשתמש או למנהל, RTL.
- **בלי מקפים ארוכים (—)** בטקסט מוצג. מקף רגיל, פסיק או נקודה במקום.
- `db/schema.sql` רץ בכל עליית קונטיינר וחייב להישאר אידמפוטנטי:
  `CREATE TABLE IF NOT EXISTS`, `ON CONFLICT DO NOTHING`, ו-`ALTER TABLE ... IF NOT EXISTS`.
- הרשאות נאכפות **בשרת**. הסתרת כפתור ב-UI היא לנוחות בלבד, לא אבטחה.
- דחיפה ישירות ל-`main`. אחרי דחיפה, לוודא שהדיפלוי ב-Railway עלה בהצלחה.

## מפה מהירה

| נתיב | תפקיד |
|---|---|
| `/` | דף הבית השיווקי |
| `/webinar`, `/thank-you` | הרשמה לוובינר הפתוח ודף התוכניות |
| `/landing/*` | 5 דפי נחיתה ייעודיים למפגשים |
| `/admin/*` | ניהול: לידים, רוכשים, לו״ז, תמחור, קופונים, אימיילים |
| `/partner` | **עמוד בנק השעות המשותף.** מוסתר, התחברות נפרדת מהאדמין |

- `src/lib/*.server.ts` — גישה למסד הנתונים ולוגיקת צד שרת
- `src/lib/*.functions.ts` — `createServerFn` עם ולידציית zod ושומרי הרשאה

## הפקת וידאו

סרטוני השיווק האנכיים נבנים מ-`scripts/video/`. **לפני שנוגעים בהם, לקרוא את
`docs/video-production.md`** - שם מתועדים המפרט, מבנה הטיימליין, כלל שתי השורות
בכתוביות, שלוש מלכודות ה-RTL השונות (libass מול Pillow) ורמות האודיו המאושרות.
